# Medium TypeScript App Benchmark Fixture

TypeScript app-shaped fixture with focused booking spend logic and unrelated generated copy.

Run:

```bash
codeprism benchmark examples/benchmarks/medium-typescript-app --query "booking spend"
```

Token counts are local estimates for comparison, not billing-grade measurements.
