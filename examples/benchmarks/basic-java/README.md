# Basic Java Benchmark Fixture

Small Java fixture for reproducible CodePrism benchmark-suite output.

Run:

```bash
codeprism benchmark examples/benchmarks/basic-java --query "server boot"
```

Token counts are local estimates for comparison, not billing-grade measurements.
