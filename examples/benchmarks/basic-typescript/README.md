# Basic TypeScript Benchmark Fixture

Small TypeScript fixture for reproducible CodePrism benchmark-suite output.

Run:

```bash
codeprism benchmark examples/benchmarks/basic-typescript --query "travel route"
```

Token counts are local estimates for comparison, not billing-grade measurements.
