# Medium Kotlin Android Benchmark Fixture

Kotlin Android-shaped fixture with settings language state and generated translation copy.

Run:

```bash
codeprism benchmark examples/benchmarks/medium-kotlin-android --query "settings language"
```

Token counts are local estimates for comparison, not billing-grade measurements.
