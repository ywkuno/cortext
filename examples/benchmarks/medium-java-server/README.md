# Medium Java Server Benchmark Fixture

Java server-shaped fixture with startup flow code and generated table noise.

Run:

```bash
codeprism benchmark examples/benchmarks/medium-java-server --query "world startup"
```

Token counts are local estimates for comparison, not billing-grade measurements.
