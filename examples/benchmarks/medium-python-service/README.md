# Medium Python Service Benchmark Fixture

Python service-shaped fixture with a focused billing path and unrelated generated catalog data.

Run:

```bash
codeprism benchmark examples/benchmarks/medium-python-service --query "invoice total"
```

Token counts are local estimates for comparison, not billing-grade measurements.
