# Basic Kotlin Benchmark Fixture

Small Kotlin fixture for reproducible CodePrism benchmark-suite output.

Run:

```bash
codeprism benchmark examples/benchmarks/basic-kotlin --query "settings locale"
```

Token counts are local estimates for comparison, not billing-grade measurements.
